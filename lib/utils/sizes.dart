import 'package:flutter/cupertino.dart';

// Vertical Margin
yMargin(double height) => SizedBox(
      height: height,
    );

// Horizontal Margin
xMargin(double width) => SizedBox(
      width: width,
    );
